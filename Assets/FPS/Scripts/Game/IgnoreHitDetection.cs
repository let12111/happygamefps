using UnityEngine;

namespace Unity.FPS.Game
{
    // ============================================================================
    // IgnoreHitDetection — пустой компонент-маркер. Сам по себе ничего не делает.
    // Логика: ProjectileStandard и другие системы при попадании ИЩУТ этот
    // компонент через GetComponent на цели. Если он есть — попадание игнорируется.
    //
    // Это паттерн «tag через компонент». Альтернатива — string-тэг Unity, но
    // компонент даёт type-safety и не путается со встроенными тэгами.
    //
    // Применение: повесить на коллайдер, через который пули должны пролетать
    // насквозь (например, прозрачные триггеры, козырьки врагов, эффекты).
    // ============================================================================
    public class IgnoreHitDetection : MonoBehaviour
    {
    }
}
